# Required secrets — generate strong random values for production.
TIKAWANG_ENV=production
TIKAWANG_AUTH_PASSWORD=change-me-strong-password
TIKAWANG_SESSION_SECRET=change-me-long-random-session-secret

# Public domain used by Caddy for automatic HTTPS
DOMAIN=key.whistlelads.com
